import { MotionProps } from "../../../motion";
import { ResolvedValues } from "../../types";
import { LayoutState, TargetProjection } from "../../utils/state";
import { DOMVisualElementOptions, HTMLMutableState } from "../types";
export declare function buildHTMLStyles(state: HTMLMutableState, latestValues: ResolvedValues, projection: TargetProjection, layoutState: LayoutState, options: DOMVisualElementOptions, transformTemplate?: MotionProps["transformTemplate"]): void;
