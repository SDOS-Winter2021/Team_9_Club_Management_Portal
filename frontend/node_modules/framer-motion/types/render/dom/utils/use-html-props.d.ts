import { MotionProps } from "../../../motion/types";
import { ResolvedValues, VisualElement } from "../../types";
export declare function useStyle(visualElement: VisualElement, props: MotionProps): ResolvedValues;
export declare function useHTMLProps(visualElement: VisualElement, props: MotionProps): any;
