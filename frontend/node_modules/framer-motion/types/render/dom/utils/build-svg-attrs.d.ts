import { SVGMutableState, DOMVisualElementOptions } from "../types";
import { ResolvedValues } from "../../types";
import { MotionProps } from "../../../motion/types";
import { LayoutState, TargetProjection } from "../../utils/state";
/**
 * Build SVG visual attrbutes, like cx and style.transform
 */
export declare function buildSVGAttrs(state: SVGMutableState, { attrX, attrY, originX, originY, pathLength, pathSpacing, pathOffset, ...latest }: ResolvedValues, projection: TargetProjection, layoutState: LayoutState, options: DOMVisualElementOptions, transformTemplate?: MotionProps["transformTemplate"]): void;
